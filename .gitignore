__pycache__/
*.pyc

# personal data (journals, music, favorites) - not source code
MyFavoriteThingsData/

# large runtime binaries, not source - keep them next to app.py locally,
# but don't commit them to the repo
ffmpeg.exe
ffplay.exe
ffprobe.exe

# PyInstaller build output - regenerate with BuildExe.bat, don't commit
build/
dist/
*.spec

# editor-local settings, not project config
.vscode/

# manual backup copies/zip attempts made while troubleshooting Netlify uploads
gdrive_app (Proto 1).zip
gdrive_app - Copy/
